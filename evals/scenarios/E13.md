# E13: Matt Pocock Skills + Superpowers + gstack all present

**Complement mode:** three — copy `evals/fixtures/matt-pocock/skills/*`, `evals/fixtures/superpowers/skills/*`, and `evals/fixtures/gstack/skills/*` into the eval workspace alongside founder-skills' own `skills/`.

**Expected skills:** `founder-start` → the relevant downstream founder-skills skill → one prepared handoff

**Fictional setup:** "Northbeam" (route-planning tool for delivery fleets) needs a specific product change — a bulk-import feature for depot addresses — to test a pricing hypothesis with a mid-market prospect. All three complements are present in the environment.

## Founder's opening message

> Northbeam needs a bulk address-import feature so I can run a pricing test with a mid-market prospect who has 200+ depots to enter manually right now. I've got Matt Pocock's skills, Superpowers, and gstack all installed in this environment. What's the plan?

## Must not

Run three equivalent planning or discovery passes for the same question (e.g. Matt `domain-modeling` **and** Superpowers `brainstorming` **and** a gstack review all independently re-deriving what the change should be). Per `docs/PLAN.md` §11 E13 and §4, the rule is one owner per step.

## Pass criteria

- `founder-start` (or the routed skill) detects and records that all three are present, without re-running a full business diagnosis if context already exists.
- The downstream skill (likely `founder-mvp`) names a scope for the bulk-import change tied to the pricing-test hypothesis, then picks **one** owner for planning/design and **one** for implementation/testing — not one of each package doing the same job in parallel.
- Produces exactly one outbound handoff (per `templates/handoff.md`'s shape) naming which package/skill owns which step, rather than three separate uncoordinated handoffs.
- Does not auto-invoke `to-spec` / `to-tickets` even though Matt Pocock Skills is present — those are explicit-invocation only (see `E14`).
- If a strategic question actually arises (not just execution), at most one of `office-hours`/`plan-ceo-review` gets named — not both, and not redundantly with a Matt/Superpowers pass covering the same ground.
